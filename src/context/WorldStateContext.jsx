import React, { createContext, useCallback, useContext, useEffect, useMemo, useState } from 'react';
import { api } from '../api';
const Context=createContext(null);
const FALLBACK={timeOfDay:'NIGHT',seasonalCycle:'NORMAL',weather:'SNOWFALL',temperature:-11,windDirection:'NE',windIntensity:2,moonPhase:'WAXING_GIBBOUS',skyState:'AURORA',runeOfTheDay:{name:'ISA',symbol:'ᛁ'},citadelState:'NORMAL',threatLevel:'I',activeEvents:[],activeEffects:[],worldScars:[],narrativeReport:'Nad murami trwa spokojna, śnieżna warta.'};
export function WorldStateProvider({children}){const[worldState,setWorldState]=useState(FALLBACK);const[loading,setLoading]=useState(true);const[presentationMode,setPresentationMode]=useState(()=>localStorage.getItem('tmd_world_presentation')||'BALANCED');const refresh=useCallback(async()=>{const r=await api.getWorldState();if(r.ok)setWorldState(r.data);setLoading(false);return r},[]);useEffect(()=>{refresh();const timer=setInterval(refresh,60000);return()=>clearInterval(timer)},[refresh]);useEffect(()=>localStorage.setItem('tmd_world_presentation',presentationMode),[presentationMode]);const reducedMotion=typeof window!=='undefined'&&matchMedia('(prefers-reduced-motion: reduce)').matches;const effectiveMode=reducedMotion?'QUIET':presentationMode;const effectsByType=useMemo(()=>(worldState.activeEffects||[]).reduce((a,e)=>((a[e.type]??=[]).push(e),a),{}),[worldState.activeEffects]);return <Context.Provider value={{worldState,loading,refresh,presentationMode,setPresentationMode,effectiveMode,reducedMotion,effectsByType}}>{children}</Context.Provider>}
export const useWorldState=()=>useContext(Context);
